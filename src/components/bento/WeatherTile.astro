---
export interface Props {
  temperature?: number;
  condition?: string;
  location?: string;
}

const { 
  temperature = 12, 
  condition = 'Pochmurnie', 
  location = 'Magazyn Główny' 
} = Astro.props;

// Mock weather data - w produkcji można podłączyć prawdziwe API
const weatherIcons: Record<string, string> = {
  'Słonecznie': '☀️',
  'Pochmurnie': '☁️',
  'Deszczowo': '🌧️',
  'Śnieżnie': '❄️',
  'Mglisto': '🌫️',
};

const icon = weatherIcons[condition] || '☁️';
---

<div class="col-span-1 row-span-1 bg-glass-bg border border-glass-border rounded-3xl p-6 backdrop-blur-xl transition-all duration-300 hover:scale-[1.02] hover:shadow-2xl hover:shadow-accent/20">
  <div class="flex items-center justify-between mb-4">
    <div class="text-3xl">{icon}</div>
    <div class="text-xs text-gray-500">TERAZ</div>
  </div>
  
  <div class="space-y-2">
    <h3 class="text-sm font-medium text-gray-400">{location}</h3>
    <div class="text-3xl font-bold text-accent font-display">{temperature}°C</div>
    <p class="text-sm text-gray-300">{condition}</p>
  </div>
  
  <div class="mt-4 pt-4 border-t border-glass-border">
    <div class="text-xs text-gray-500">
      Dostęp 24/7 bez względu na pogodę
    </div>
  </div>
</div>